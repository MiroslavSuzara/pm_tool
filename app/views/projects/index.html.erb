<h1>Projects</h1>
  <table class="table table-hover">
    <tr>    
      <th>Project</th>
      <th>Body</th>
      <th>Due Date</th>
      <th class="actions">Actions</th>
    </tr>
      <% @projects.each do |t| %>
    <tr>
      <td><%= link_to t.title, project_path(t) %></td>
      <td><%= t.description %></td>
      <td><%= t.due_date %></td>
      <td class="actions">
          <div class="btn-group">
            <%= link_to "Edit", edit_project_path(t), class: "btn btn-default" %> 
            <%= link_to "Delete", t, method: :delete, data:{confirm: "Are you sure you want to delete this request?"}, class: "btn btn-default" %> 
          </div>
        </td>
    </tr>
  <% end %>
  </table>
  <hr>